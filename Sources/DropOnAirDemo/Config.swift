import SwiftUI

/// Backend URL, points to the demo backend (droponair-demo-backend, port 8180)
let backendURL = "http://localhost:8180"

/// DropOnAir credentials, replace with values from panel.droponair.com
let droponairAppId        = "YOUR_APP_ID"
let droponairPublicApiKey = "YOUR_PUBLIC_API_KEY"
